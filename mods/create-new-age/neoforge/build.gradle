dependencies {
    compileOnly(project(":common"))
    neoForge("net.neoforged:neoforge:${project.neoforgeVersion}")

    implementation("com.simibubi.create:create-${rootProject.minecraftVersion}:${rootProject.createVersion}:slim") { transitive = false }
    implementation("net.createmod.ponder:ponder-neoforge:${ponderVersion}+mc${minecraftVersion}")
    compileOnly("dev.engine-room.flywheel:flywheel-neoforge-api-${minecraftVersion}:${flywheelVersion}")
    runtimeOnly("dev.engine-room.flywheel:flywheel-neoforge-${minecraftVersion}:${flywheelVersion}")
    api("com.tterrag.registrate:Registrate:${rootProject.registrateVersion}")

    if (enableCraftsAndAdditions.toBoolean()) {
        runtimeOnly("maven.modrinth:createaddition:neoforge-${minecraftVersion}-${craftsAndAdditionsVersion}")
    }
    if (enableAE2.toBoolean()) {
        runtimeOnly("org.appliedenergistics:appliedenergistics2:${appliedEnergistics2Version}")
    }

    compileOnly("io.github.llamalad7:mixinextras-common:0.5.0")
    annotationProcessor(implementation(include("io.github.llamalad7:mixinextras-neoforge:0.5.0")))

    // Computer Craft stuffs
    compileOnly("cc.tweaked:cc-tweaked-${rootProject.minecraftVersion}-forge-api:${rootProject.cctVersion}")
    if (enableCCT.toBoolean()) {
        runtimeOnly("cc.tweaked:cc-tweaked-${rootProject.minecraftVersion}-forge:${rootProject.cctVersion}")
    }
}

processResources {
    from(project(":common").sourceSets.main.resources)

    var replaceProperties = [
            "version"         : version.replaceAll("\\+.*-mc", "\\+mc"),
            "minecraftVersion": minecraftVersion,
            "neoforgeVersion" : neoforgeVersion,
            "eslVersion"      : eslVersion,
            "createVersion"   : createVersionShort,
            "cctVersion"      : cctVersion,
            "objLoader"       : objLoader,
    ]

    inputs.properties replaceProperties

    filesMatching(["META-INF/neoforge.mods.toml", "assets/**/*.json"]) {
        expand replaceProperties
    }
}

compileJava {
    source(project(":common").sourceSets.main.allSource)
}

var modId = rootProject.modId


loom {
    runs {
        datagen {
            data()
            programArgs("--all", "--mod", "${modId}")
            programArgs("--output", file("src/generated/resources").absolutePath)
            programArgs("--existing", file("../common/src/main/resources").absolutePath)
        }
    }
}

// https://modmuss50.github.io/mod-publish-plugin/
publishMods {
    file = remapJar.archiveFile
    changelog = rootProject.silk.changelog
    type = STABLE
    version = "$modVersion+mc$minecraftVersion"
    displayName = "$modVersion for Neoforge $minecraftVersion"
    modLoaders.add("neoforge")

    curseforge {
        accessToken = providers.environmentVariable("CURSEFORGE_TOKEN")
        projectId = "905861"
        clientRequired = true
        serverRequired = true
        requires("create")
        minecraftVersions.add(minecraftVersion)
    }

    modrinth {
        accessToken = providers.environmentVariable("MODRINTH_TOKEN")
        projectId = "FTeXqI9v"
        requires { // Requires Create
            id = "LNytGWDc"
        }
        minecraftVersions.add(minecraftVersion)
    }
}
